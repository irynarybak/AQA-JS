```markdown
-  Встановити гіт : https://git-scm.com/downloads
-  Задати ім'я юзера : git config --global user.name "name"
-  Задати ємейл юзера : git config --global user.email "email"
-  Створити SSH-key : ssh-keygen -t ed25519 -C "your_email@example.com"
```
